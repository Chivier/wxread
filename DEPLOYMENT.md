# 本 fork 的部署方式

Actions 通过 `folder_reader.py` 打开微信读书「在读列表」，使用真实网页阅读器按页继续阅读；不调用上游 `main.py` 的随机书目/章节模板。

## 固定一本，读完才换

每次运行重新获取文件夹内的书单，但始终继续加密检查点记录的当前书，不按日期轮换。只有网页出现明确的「全书完」标记，才记录为读完并选下一本。付费页面、无下一页按钮、登录或网络错误均停止，不算读完。连载读到最新章节、但没有「全书完」时也停止。

在手机 App 中移入书籍即加入候选；移出当前书视为主动取消，后续会选另一未读完书。已完成的书不会反复阅读来填满时长。文件夹为空或全部读完时停止。标题包含「三体」「三體」「Three Body / Three-Body」的书始终排除。

跨段、跨天继续同一本，具体阅读位置由微信读书云端同步。检查点保存当前书、完成记录、每日累计和刷新后的浏览器登录状态，用独立密钥认证加密后保存在 Actions artifacts。Cookie、书名、截图和书架内容不进入公开日志。检查点下载或解密失败时停止，不重置进度。

## 每天六小时目标

北京时间每天 01:07（UTC 前一天 17:07）启动，串行运行三个约两小时的 session；每个 job 有 200 分钟上限。不会并行读多本。重跑按北京时间当日累计计数，达到 `21600` 秒后停止。手动短测也计入当日累计。

时长依据微信读书成功 `read` 响应所对应请求中的 `rt` 统计，达到目标即结束，单次心跳可能有少量误差。安装、加载、网络等待不算阅读秒数。此统计不等同于排行榜或挑战赛最终入账，不保证服务端认可完整六小时。登录失效、无法继续、书单读完等情况不会强行凑时长。

手动任务跨过北京时间零点时会继续当前书，每段累计不会清零；每次成功响应的时长计入收到响应时的北京时间日期。到达当日上限即结束，不等待零点。手动与定时任务共用串行锁和每日累计。

## GitHub 设置

Settings → Secrets and variables → Actions：

| 类型 | 名称 | 用途 |
| --- | --- | --- |
| Secret | `WXREAD_CURL_BASH` | 从本人阅读页面复制的 cURL (bash)，用于首次登录 |
| Secret | `WXREAD_STATE_KEY` | 检查点加密密钥，不能随意更换 |
| Variable | `READ_FOLDER_URL` | 指定文件夹的完整网页地址 |
| Variable | `READ_FOLDER_NAME` | 校验文件夹名称 |
| Variable | `READ_INITIAL_BOOK_ID` | 仅首次初始化时选择哪一本；已有进度优先 |
| Variable | `DAILY_READ_SECONDS` | 默认 `21600`（六小时） |
| Variable | `REQUIRE_READER_STATE` | 初始化后设为 `true`，丢失检查点时停止 |

每段结束（包括正常异常退出）上传加密检查点，保留 90 天，每次恢复最新版本。逐次成功请求都在 runner 内原子更新加密文件；runner 被硬中断时可能退回上一次已上传的检查点，微信读书云端进度仍以网站为准。

手动 Run workflow：`read_num=2` 仅执行一次约一分钟短测；留空则执行三段，并受每日总量限制。登录失效时更新 `WXREAD_CURL_BASH`；脚本发现 Secret 已变化后只重建登录会话，保留当前书、已完成记录和每日累计，不会删除整份进度。

## 验证

```sh
pip install cryptography==50.0.1
python -m unittest discover -s tests -v
```

运行时使用 Playwright 1.63.0 和对应 Chromium。上游 Docker / `main.py` 仍保留原有行为，不具有本文件夹与时长限制。请使用本 fork 的 Actions 工作流。
